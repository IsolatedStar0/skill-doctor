[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install -e \"backend[api]\"",
]

[start]
cmd = "python -m uvicorn backend.skilldoctor.api:app --host 0.0.0.0 --port ${PORT}"
